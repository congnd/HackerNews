import XCTest
import Api

/// This is for API testing which requires a real connection to the API server.
/// These tests should not be run on CI.
/// You can run these tests on your mac instead of an iOS Simulator for speed.
class EndpointTests: XCTestCase {
  func test() {
//    let exp = XCTestExpectation()
//
//    Loader().request(target: Endpoint.Item(id: 26301395)) { result in
//      print(result)
//    }
//
//    wait(for: [exp], timeout: 4)
  }
}
